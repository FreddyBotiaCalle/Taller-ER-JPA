package com.taller.erjpa.repository;

import com.taller.erjpa.model.Rol;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RolRepository extends JpaRepository<Rol, Long> {
}
